mission:
  name: "Master模式"
  prompt:
    system: |
      当前进程是Master控制进程，不初始化或调用任何模型后端。
      使用PDB中的Master API、任务、终端、状态和文件管理命令进行控制；模型对话与Agent工作循环命令在此模式下不可用。
    tips:
      allways: []
      random: []

tools:
  RunTestCases:
    test_dir: "{OUT}/tests"
  ignore_tools: ["WorkDiff", "WorkCommit"] # 去掉  "RunBashCommand"

template: Null

stage: []

un_write_dirs: []

guide_doc:
  enable: false
